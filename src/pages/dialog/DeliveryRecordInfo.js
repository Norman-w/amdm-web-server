import React from 'react';
import PropTypes from 'prop-types';

const DeliveryRecordInfo = props => {
  const {record} = props;
  return (
    <div>

    </div>
  );
};

DeliveryRecordInfo.propTypes = {

};

export default DeliveryRecordInfo;
